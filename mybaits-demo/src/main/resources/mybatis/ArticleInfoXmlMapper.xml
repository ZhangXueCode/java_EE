<?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.ArticleInfoXmlMapper">
        <select id="select">
                select article_info.*,user_info.username from user_info,article_info where user_info.id = article_info.uid
        </select>

</mapper>